console.log(
	"sample index.js file, whose's attributes you will modify with the `main()` function in the `src/index.ts` file",
	"assuming, ofc, that you run the script from this directory (the filename is relative from where you call the script",
	"just see the index.js#main()"
);
